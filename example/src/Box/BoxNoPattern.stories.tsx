import * as React from 'react';
import { Box } from './Box';

export default {
  title: 'Box/Error/NoPattern',
  component: Box,
  parameters: {
    matrix: {},
  },
};

export const Basic = () => <Box>TEXT</Box>;
