import * as React from 'react';
import { Box } from './Box';

export default {
  title: 'Box/Error/NoDefaultComonentAndPattern',
  parameters: {
    matrix: {},
  },
};

export const NoDefaultComonentAndPattern = () => <Box>TEXT</Box>;
